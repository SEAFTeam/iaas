entities:
  seaf.ta.reverse.vmwarecloud.egws:
    menu: >
      (
          $domain := '';
          $domain_matcher := $eval('/^' & $domain & '.*/');
      
          $egws_entity := "seaf.ta.reverse.general".config.entities.egws.model;
          $egws_view := "seaf.ta.reverse.general".config.entities.egws.view;
          $vdcs_entity := "seaf.ta.reverse.general".config.entities.vdcs.model;
          $dcs_entity := "seaf.ta.reverse.general".config.entities.dc.model;
          $orgs_entity := "seaf.ta.reverse.general".config.entities.orgs.model;
          $vdcgroups_entity := "seaf.ta.reverse.general".config.entities.vdcgroups.model;
      
          $egws := $lookup($, $egws_entity)[$domain_matcher($keys())];
          $dcs := $lookup($, $dcs_entity)[$domain_matcher($keys())];
          $vdcs := $lookup($, $vdcs_entity)[$domain_matcher($keys())];
          $vdcgroups := $lookup($, $vdcgroups_entity)[$domain_matcher($keys())];
          $orgs := $lookup($, $orgs_entity)[$domain_matcher($keys())];
      
          $config := "seaf.ta.reverse.general".config;
          $lists := [$orgs.$spread().(
              $dcid := $.*.DC;
              $dcname := $dcs.$spread()[$keys() = $dcid].*.title;
              {
                  'location': $config.arch_menu & '/' & $dcname & '/Облако VMware/Реестры/Шлюзы',
                  'link': 'entities/' & $egws_view & '/list?domain=' & $domain
              }
          )];
          /*$static := [
              {
                  'location': $config.arch_menu & '/Cloud.ru/Облако VMware/Реестры/Шлюзы (Edge Gateway)',
                  'link': 'entities/' & $egws_view & '/list?domain=' & $domain
              }
          ];*/
          $dynamic_vdc := $egws.$spread()[*.vdcgroup_id = null].(
              $id := $keys()[0];
              $name := $.*.name;
              $vdcname := $.*.vdc_name;
              $vdc_id := $.*.vdc_id;
              $org_id := $.*.org_id;
              $vdcgroup := $vdcgroups.$spread()[$vdc_id in *.vdcs.id];
              $vdcgroupname := $vdcgroup.*.name;
              $org := $orgs.$spread()[$keys() = $org_id];
              $orgname := $org.*.name;
              $dc := $dcs.$spread()[$keys() = $org.*.DC];
              $exists($vdcgroup) ? 
                  [
                      {
                          'location': $config.arch_menu & '/' & $dc.*.title & '/Облако VMware/Организации/' & $orgname & '/' & $vdcgroupname & '/Edge Gateways/' & $name,
                          'link': 'entities/' & $egws_view & '/schema_vdcgroup?id=' & $id
                      },
                      {
                          'location': $config.arch_menu & '/' & $dc.*.title & '/Облако VMware/Организации/' & $orgname & '/' & $vdcgroupname & '/Edge Gateways',
                          'link': 'entities/' & $egws_view & '/list?vdcgroup_id=' & $vdcgroup.*.id
                      }     
                  ]
                  :
                    [
                        {
                            'location': $config.arch_menu & '/' & $dc.*.title & '/Облако VMware/Организации/' & $orgname & '/' & $vdcname & '/Edge Gateways/' & $name,
                            'link': 'entities/' & $egws_view & '/schema?id=' & $id
                        },
                        {
                            'location': $config.arch_menu & '/' & $dc.*.title & '/Облако VMware/Организации/' & $orgname & '/' & $vdcname & '/Edge Gateways',
                            'link': 'entities/' & $egws_view & '/list?vdc_id=' & $vdc_id
                        }     
                    ]
          );
          $dynamic_vdcgroup := $egws.$spread()[*.vdc_id = null].(
              $id := $keys()[0];
              $name := $.*.name;
              $vdcgroupname := $.*.vdcgroup_name;
              $vdcgroup_id := $.*.vdcgroup_id;
              $org_id := $.*.org_id;
              $org := $orgs.$spread()[$keys() = $org_id];
              $orgname := $org.*.name;
              $dc := $dcs.$spread()[$keys() = $org.*.DC];
              [
                  {
                      'location': $config.arch_menu & '/' & $dc.*.title & '/Облако VMware/Организации/' & $orgname & '/' & $vdcgroupname & '/Edge Gateways/' & $name,
                      'link': 'entities/' & $egws_view & '/schema_vdcgroup?id=' & $id
                  },
                  {
                      'location': $config.arch_menu & '/' & $dc.*.title & '/Облако VMware/Организации/' & $orgname & '/' & $vdcgroupname & '/Edge Gateways',
                      'link': 'entities/' & $egws_view & '/list?vdcgroup_id=' & $vdcgroup_id
                  }     
              ]
          );
          $dynamic := $append($dynamic_vdc, $dynamic_vdcgroup);
          $append($lists, $distinct($dynamic))
      )