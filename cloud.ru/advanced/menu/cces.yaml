entities:
  seaf.ta.reverse.cloud_ru.advanced.cces:
    menu: >
      (
          $config := "seaf.ta.reverse.general".config;
          $count($$."seaf.ta.reverse.cloud_ru.advanced.cces") > 0 ? [{
                      /* Размещение в пользовательском меню */
                      "location": $config.arch_menu & "/Cloud.ru/Advanced/Реестры/Cloud Container Engines",
                      /* URI представления context см. ниже.*/
                      "link": "entities/seaf.ta.reverse.cloud_ru.advanced.cces/table_view"
          }];
      )