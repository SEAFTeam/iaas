entities:
  seaf.ta.reverse.cloud_ru.advanced.elbs:
    menu: >
      (
          $config := "seaf.ta.reverse.general".config;
          $count($$."seaf.ta.reverse.cloud_ru.advanced.elbs") > 0 ? [{
                      /* Размещение в пользовательском меню */
                      "location": $config.arch_menu & "/Cloud.ru/Advanced/Реестры/Elastic Load Balancers",
                      /* URI представления context см. ниже.*/
                      "link": "entities/seaf.ta.reverse.cloud_ru.advanced.elbs/table_view"
          }];
      )