entities:
  seaf.ta.reverse.cloud_ru.advanced.peerings:
    menu: >
      (
          $config := "seaf.ta.reverse.general".config;
          $count($$."seaf.ta.reverse.cloud_ru.advanced.peerings") > 0 ? [{
                      /* Размещение в пользовательском меню */
                      "location": $config.arch_menu & "/Cloud.ru/Advanced/Реестры/Peerings",
                      /* URI представления context см. ниже.*/
                      "link": "entities/seaf.ta.reverse.cloud_ru.advanced.peerings/table_view"
          }];
      )