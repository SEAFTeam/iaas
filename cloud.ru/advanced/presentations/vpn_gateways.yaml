entities:
  seaf.ta.reverse.cloud_ru.advanced.vpn_gateways:
    presentations:
      table_view:
        type: table
        headers:
          - value: name
            text: Gateway name
            width: 200
            sortable: true
            link: link
          - value: ip
            text: IP адрес
          - value: subnet
            text: Подсеть
            width: 150
          - value: vpc
            text: VPC
            width: 150
          - value: dc
            text: DC/IaaS
            width: 150
        source: >
          (
              $ctx := $params;
              $domain_matcher := $eval('/^' & $ctx.domain & '.*/');
              $cvpngws := $."seaf.ta.reverse.cloud_ru.advanced.vpn_gateways" ~> $each(function($v, $k){$domain_matcher($k) ? {$k:$v}});
              $cvpnconn := $."seaf.ta.reverse.cloud_ru.advanced.vpn_connections" ~> $each(function($v, $k){$domain_matcher($k) ? {$k:$v}});
              $csubnets := $."seaf.ta.reverse.cloud_ru.advanced.subnets" ~> $each(function($v, $k){$domain_matcher($k) ? {$k:$v}});
              $cvpcs := $."seaf.ta.reverse.cloud_ru.advanced.vpcs" ~> $each(function($v, $k){$domain_matcher($k) ? {$k:$v}});
              $cdcs := $."seaf.ta.services.dc" ~> $each(function($v, $k){$domain_matcher($k) ? {$k:$v}});
              $cvpngws.$spread().[(
                  $self := $;
                  $subnet := $csubnets.$spread()[$.*.id = $self.*.subnet_id].*.name;
                  $vpc := $cvpcs.$spread()[$.*.id = $self.*.vpc_id].*.name;
                  $connections := $cvpnconn.$spread()[*.gw_id = $self.$keys()];
                  $vpngw_dc := $.*.DC;
                  $dc := $cdcs.$spread()[$keys()[0] = $vpngw_dc].*.dc_name;
                  {
                      'name': $self.*.name,
                      'ip': $self.*.ip_address,
                      'subnet': $subnet,
                      'vpc': $vpc,
                      'dc': $dc,
                      'link': '/entities/seaf.ta.reverse.cloud_ru.advanced.vpn_gateways/vpngw_md?id=' & $self.$keys()[0] & '&domain=' & $ctx.domain
                  }
              )]
          )

      vpngw_md:
        type: markdown
        template: templates/vpngw.md
        source: >
          (
             $ctx := $params;
             $domain_matcher := $eval('/^' & $ctx.domain & '.*/');
             $cvpngw := $."seaf.ta.reverse.cloud_ru.advanced.vpn_gateways" ~> $each(function($v, $k){$domain_matcher($k) ? {$k:$v}});
             $cvpngw := $cvpngw[$keys() = $ctx.id];
             $csubnets := $."seaf.ta.reverse.cloud_ru.advanced.subnets" ~> $each(function($v, $k){$domain_matcher($k) ? {$k:$v}});
             $cvpcs := $."seaf.ta.reverse.cloud_ru.advanced.vpcs" ~> $each(function($v, $k){$domain_matcher($k) ? {$k:$v}});
             $subnet := $csubnets.$spread()[$.*.id = $cvpngw.*.subnet_id].*.name;
             $vpc := $cvpcs.$spread()[$.*.id = $cvpngw.*.vpc_id].*.name;
             $cdcs := $."seaf.ta.services.dc";
             $vpngw_dc := $cvpngw.*.DC;
             $dc := $cdcs.$spread()[$keys()[0] = $vpngw_dc].*.dc_name;
             {
                 'name': $cvpngw.*.name,
                 'ip': $cvpngw.*.ip_address,
                 'subnet': $subnet,
                 'vpc': $vpc,
                 'dc': $dc,
                 'id': $ctx.id
             }
          )

      vpn_connections:
        type: table
        headers:
          - value: name
            text: Название VPN соединения
          - value: remote_gw_ip
            text: IP адрес удаленного узла
          - value: remote_subnets
            text: Удаленные сети
          - value: branch
            text: Удаленный офис/ЦОД
        source: >
          (
              $ctx := $params;
              $domain_matcher := $eval('/^' & $ctx.domain & '.*/');
              $realty := $."seaf.ta.services.dc".$spread().($);
              $realty := $append($realty, $."seaf.ta.services.office".$spread().($));
              $realty := $merge($realty) ~> $each(function($v, $k){$domain_matcher($k) ? {$k:$v}});
              /* $realty := $.$spread()[$keys()[0] = "seaf.ta.services.dc" or $keys()[0] = "seaf.ta.services.office"]; */
              $cvpngw := $."seaf.ta.reverse.cloud_ru.advanced.vpn_gateways" ~> $each(function($v, $k){$domain_matcher($k) ? {$k:$v}});
              $cvpngw := $cvpngw[$keys() = $ctx.id];
              $connections := $."seaf.ta.reverse.cloud_ru.advanced.vpn_connections" ~> $each(function($v, $k){$domain_matcher($k) ? {$k:$v}});
              $connections := $connections[*.gw_id = $cvpngw.*.id];
              $connections.$spread().(
                  $self := $;
                  $matcher := /\.*name$/;
                  $branch := $realty.$spread().*.$spread()[$keys()[0] = $self.*.branch_id].*.$spread()[$matcher($keys()[0])].*;
                  {
                      'name': $self.*.name,
                      'remote_gw_ip': $self.*.remote_gw_ip,
                      'remote_subnets': $join($self.*.remote_subnets, ', '),
                      'branch': $branch
                  }
              )
          )