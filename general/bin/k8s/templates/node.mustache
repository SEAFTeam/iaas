    {{#items}}
    {{entity}}.{{metadata.uid}}:
        id: {{metadata.uid}}
        title: {{metadata.name}}
        version: {{status.node_info.kubelet_version}}
        k8s_cluster_id: {{parent.entity}}.{{parent.item.name}}
        architecture: {{status.node_info.architecture}}
        cpu: {{status.allocatable.cpu}}
        {{metadata.labels.node-role.kubernetes.io/control-plane}}is_control_plane: true{{metadata.labels.node-role.kubernetes.io/control-plane}}
        addresses:
        {{#status.addresses}}
            - address: {{address}}
              type: {{type}}
        {{/status.addresses}}
        {{#metadata.labels}}
        labels:
        {{#iterator}}
            - @kv{{{.}}}
        {{/iterator}}
        {{/metadata.labels}}

    {{/items}}
