{{#items}}
    {{entity}}.{{metadata.uid}}:
        deployment_id: {{metadata.uid}}
        name: {{metadata.name}}
        namespace: {{parent.entity}}.{{parent.item.metadata.uid}}
        namespace-name: {{parent.item.metadata.name}}
        update_strategy: {{spec.strategy.type}}
        replicas: {{spec.replicas}}
{{/items}}
